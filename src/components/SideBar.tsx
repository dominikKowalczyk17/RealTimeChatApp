import { AccountDetails } from "./AccountDetails";
import { Messages } from "./Messages";

export const SideBar = () => {
  return (
    <div>
      <AccountDetails />
      <Messages />
    </div>
  );
};
